import 'slickgrid/plugins/slick.draggablegrouping';
import { Extension, GridOption, SlickDraggableGrouping, SlickEventHandler } from '../interfaces/index';
import { ExtensionUtility } from './extensionUtility';
import { SharedService } from '../services/shared.service';
export declare class DraggableGroupingExtension implements Extension {
    private readonly extensionUtility;
    private readonly sharedService;
    private _addon;
    private _draggableGroupingOptions;
    private _eventHandler;
    constructor(extensionUtility: ExtensionUtility, sharedService: SharedService);
    get eventHandler(): SlickEventHandler;
    dispose(): void;
    /**
     * Bind/Create different plugins before the Grid creation.
     * For example the multi-select have to be added to the column definition before the grid is created to work properly
     */
    create(gridOptions: GridOption): SlickDraggableGrouping | null;
    /** Get the instance of the SlickGrid addon (control or plugin). */
    getAddonInstance(): SlickDraggableGrouping | null;
    /** Register the 3rd party addon (plugin) */
    register(): SlickDraggableGrouping | null;
}
