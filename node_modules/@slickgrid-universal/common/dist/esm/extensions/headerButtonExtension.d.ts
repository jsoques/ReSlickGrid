import 'slickgrid/plugins/slick.headerbuttons';
import { Extension, SlickEventHandler, SlickHeaderButtons } from '../interfaces/index';
import { ExtensionUtility } from './extensionUtility';
import { SharedService } from '../services/shared.service';
export declare class HeaderButtonExtension implements Extension {
    private readonly extensionUtility;
    private readonly sharedService;
    private _eventHandler;
    private _addon;
    private _headerButtonOptions;
    constructor(extensionUtility: ExtensionUtility, sharedService: SharedService);
    get eventHandler(): SlickEventHandler;
    dispose(): void;
    /** Get the instance of the SlickGrid addon (control or plugin). */
    getAddonInstance(): SlickHeaderButtons | null;
    /** Register the 3rd party addon (plugin) */
    register(): SlickHeaderButtons | null;
}
