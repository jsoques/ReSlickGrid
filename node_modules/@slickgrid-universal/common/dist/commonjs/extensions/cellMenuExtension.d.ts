import 'slickgrid/plugins/slick.cellmenu';
import { Column, Extension, SlickCellMenu, SlickEventHandler } from '../interfaces/index';
import { ExtensionUtility } from './extensionUtility';
import { SharedService } from '../services/shared.service';
import { TranslaterService } from '../services';
export declare class CellMenuExtension implements Extension {
    private readonly extensionUtility;
    private readonly sharedService;
    private readonly translaterService?;
    private _addon;
    private _cellMenuOptions;
    private _eventHandler;
    private _locales;
    constructor(extensionUtility: ExtensionUtility, sharedService: SharedService, translaterService?: TranslaterService | undefined);
    get eventHandler(): SlickEventHandler;
    dispose(): void;
    /** Get the instance of the SlickGrid addon (control or plugin). */
    getAddonInstance(): SlickCellMenu | null;
    /** Register the 3rd party addon (plugin) */
    register(): SlickCellMenu | null;
    /** Translate the Cell Menu titles, we need to loop through all column definition to re-translate them */
    translateCellMenu(): void;
    /**
     * @return default Action Cell Menu options
     */
    private getDefaultCellMenuOptions;
    /**
     * Reset all the internal Menu options which have text to translate
     * @param grid menu object
     */
    private resetMenuTranslations;
    sortMenuItems(columnDefinitions: Column[]): void;
}
